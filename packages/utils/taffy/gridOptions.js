
export default {

	// accepts any unit value (default: '30px')
	gutter: '20px',

	// accepts a boolean value (default: false)
	flex: true,

	// accepts a integer value (default: -1 for auto cycle)
	cycle: -1,

	// accepts a string of either 'row' or 'column' (default: row)
	offsetDir: 'row'
}